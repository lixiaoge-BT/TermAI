# 2026-09-14

## 重新打包 dl-20260908d（含生产环境二次确认回归修复）
- 之前 dl-20260908c 打包后，又给 safety.ts 加了一处回归修复：生产环境下「低危写操作」也需二次确认
  （`requireConfirmation = rank>=high || (isProd && rank>=low)`，safety.ts:823）。源码已改、测试已绿（221/0），
  但 c 包不含它 → 重新构建打包为 **dl-20260908d**。
- md5：Setup `a9bd0808d7719b49d95ecbfd536cbdaf`、便携版 `40631c1abebaa8e685b8c0716e76feb3`。
- asar 核对标记：`生产环境`×4 / `FLUSHALL`×1 / `node_modules`×9 均在 → 新 safety.ts 已入包。

## 环境坑：Bash 工具 shell 丢了 coreutils 的 PATH
- 新会话里 `npm`/`npx`/`ls`/`tail`/`head`/`dirname` 全报 127（exit 127），但 `which node/npm/npx` 能找到文件。
  根因：WorkBuddy 的 `shell-runtime-bash-env.sh` 启动脚本失败（`dirname: command not found` / `cd: null directory`），
  没把 PortableGit 的 coreutils 加进 PATH；而 `npm` 脚本内部依赖 `dirname`，所以一执行 npm 就 127。
- **修复（每次命令前加，shell 状态不跨命令持久）**：
  `export PATH="/c/Users/raotengke/.workbuddy/binaries/PortableGit/versions/1.2.0/usr/bin:$PATH"`
  加完 `npm run build` / `electron-builder` / `ls` / `tail` 全部恢复正常。
- 注意：stderr 里那两行 shim 报错是启动噪声，不影响实际命令执行（exit 0 即可信）。

## 用户关于「系统漏洞是否都修了」的澄清
- 明确告知：只修了 **AI 助手 / Agent 接管** 那一批问题（safety.ts 风险判定重写、ai.ts 截断/错误码/重试、
  AISidebar 性能与标签、store/agent.ts 截断处理），**未做整系统安全审计**
  （renderer 沙箱 contextIsolation/nodeIntegration、IPC 越权、凭证明文存储、deep link 命令注入、
  XSS sink、命令执行沙箱兜底均未审计）。
- 已向用户提议：是否开一轮正式整系统安全审计。待用户确认。
- `D:\Trae\release\0.1.0` 旧构建目录仍卡在 Windows Defender 共享锁（错误码 32），待用户在安全中心
  关实时防护或给 `D:\Trae\release` 加排除项后才能删（已保留 scripts/_del_dir.py 用 ctypes 删）。

## Agent 命令在终端里「有输出没命令」修复
- 用户反馈：Agent 接管/批量执行后，终端只看到 hash 等输出，看不到实际执行的 `docker run` 命令。
- 根因：`execCommand` 用 BEGIN/EXIT/END 哨兵包装命令，`agentEcho.ts` 的回显区间抑制
  把包装脚本连同命令回显一整段丢弃，终端里只剩输出。
- 修复：
  - `agentEcho.ts`：`beginAgentEchoFilter(sessionId, tags, command?)` 新增可选展示命令参数，
    命中 BEGIN 哨兵后、真实输出前补回 `$ <命令>\r\n`；多行命令折叠成单行，超长 240 字符截断。
  - `services/agent.ts`：`execCommand` 下发命令时把原始命令传给过滤窗口。
  - 手动点 CommandCard「执行」不走哨兵包装，不受影响（原本就有 PTY 回显）。
- 验证：tsc 0 / eslint 0 / 334 tests（新增 2 条 agentEcho 用例）全绿。
- 已重新打包为 **dl-20260908e**。

## 应用图标改圆润（用户要求所有图标圆润）
- 用户要求：应用 logo 改为圆润，安装包/状态栏/任务栏/所有图标都圆润。
- 做法：写 `scripts/make-rounded-icons.py`（隔离 venv：python envs/icon-build + Pillow）。
  源图 `assets/icons/icon-1024.png` 先裁正方形居中，再套圆角矩形遮罩（radius_ratio=0.28 ≈ 现代 app 圆角），
  重采样 lanczos 输出 16/24/32/48/64/128/256/512/1024 PNG + 多尺寸 `icon.ico`（含 256/128/64/48/32/24/16）。
- 坑：第一次生成 `.ico` 首帧是 16x16，electron-builder 报 `icon.ico must be at least 256x256` →
  改成 256 为首帧后正常。
- electron-builder 用 `win.icon=assets/icons/icon.ico` 作为安装包图标；`scripts/afterPack.mjs` 用 rcedit
  把同一 `icon.ico` 写入 `win-unpacked/TermAI.exe`（任务栏/桌面/状态栏图标）。两处都是圆角的，全覆盖。
- 验证：PNG 四角 alpha=0（透明）、边缘随圆角曲线 alpha 渐变 → 圆角生效；`.ico` 含 256 帧。
- 打包：`npm run build` + electron-builder 到 **dl-20260914b**（dl-20260914a 因失败残留被 Defender 锁 app.asar，
  改用全新目录 b 才成功）。
  - md5：Setup `1a9a8ca1c5d43c3ab52971d26dc47583`、便携版 `11951df3237fe84c9aabfb06b6b5bf29`。
  - 注：dl-20260914a 是半残失败产物（残留 win-unpacked），仍卡 Defender 锁，待用户安全中心加排除项后才能清。
- 残留清理：临时 `scripts/verify-exe-icon.py` 已删；`make-rounded-icons.py` 保留作图标重生成工具。

## Agent 终端补回命令不应带 `$` 前缀
- 用户反馈：Agent 执行后终端显示 `$ find ...` / `$ du ...`，与手输命令形式不一致。
- 根因：`agentEcho.ts` 补回显示时用了 `$ <命令>\r\n`。
- 修复：`agentEcho.ts:156` 改为 `<命令>\r\n`；同步更新 `agentEcho.test.ts` 两条断言。
- 验证：334 tests 全绿；asar 内渲染层 bundle 确认已无 `"$ "` 字面量，`TERMAI_` 上下文显示为 `\r\n${o}\r\n`。
- 重新打包为 **dl-20260914c**：Setup `80d34613546d6695d7efa2376ccdf3ef`、便携版 `aefabc496f2d88ce73d4b04c8e66470b`。

## Agent 终端补回命令应紧跟提示符，不再前置换行
- 用户反馈：Agent 执行后命令显示在提示符上方单独一行，要求像手动输入一样直接跟在 `[root@mgt ~]#` 后面。
- 根因：`agentEcho.ts` 在 BEGIN 哨兵后补的是 `\r\n<命令>\r\n`，导致光标先换行再显示命令。
- 修复：`agentEcho.ts:157` 去掉前置 `\r\n`，改为 `<命令>\r\n`；无展示命令时仍只补 `\r\n` 防提示符成对。
  同步更新 `agentEcho.test.ts` 两条用例：提示符先于窗口到达并透传，命令直接补在已有提示符后。
- 验证：334 tests 全绿；asar 内渲染层 bundle 确认：
  - dl-20260914c 旧逻辑：`a="\\r\\n"+(o?"${o}\\r\\n":"")+...`
  - dl-20260914d 新逻辑：`a=(o?"${o}\\r\\n":"\\r\\n")+...`
- 重新打包为 **dl-20260914d**：Setup `4152361103a460d9a05cf06c5caf25b2`、便携版 `9eacee22687760fede960af242578798`。

## Agent 去重闸门：写操作子命令第 2 次即拒，不再被 `rm; du` 借写清族计数
- 用户反馈：Agent 反复执行几乎相同的 `rm -rf /var/log/...; du -sh ...` 复合命令（截图 11/12 步）。
- 根因（两处）：
  1. `createDupTracker` 中写命令执行后会 `fam.clear()`，把同条命令里 `du` 的族计数清零；
  2. 所有子命令统一阈值 ≥2，导致 `rm` 子命令在第 2 次执行时仍未触发逐字去重。
- 修复（`src/services/agent.ts`）：
  - 提取 `isStateChangingSegment(seg)`，`isStateChanging` 复用它；`cmdFamily` 跳过写 segment，取第一个只读 segment 归族。
  - `createDupTracker.isExactDup` 改为双轨：写操作子命令已成功 ≥1 次即视为整条重复；只读子命令仍保持 ≥2 次。
  - `record` 中去掉 `fam.clear()`，写 segment 不再清空只读族计数。
- 测试（`src/services/agent.test.ts`）：
  - `cmdFamily` 用例同步改为数组/字符串断言；新增「复合命令跳过写 segment」用例。
  - 新增：写操作第 2 次拒绝、写后族计数不清零、截图 `rm; du` 第 2 次因 `rm` 重复被拒。
- 验证：337 tests 全绿；asar 渲染层 bundle 出现 `a.n>=1&&a.lastOk` 写操作阈值模式，旧包 dl-20260914d 无此模式。
- 重新打包为 **dl-20260914e**：Setup `06864cf9633998b1e17ab26db39f2f2f`、便携版 `98b7c611dd9341e9af69478fd3a15c9e`。

## Agent 引擎程序端优化（P0 四项，按用户确认的顺序 ①→②→③→⑥）

背景：用户问「接管不够智能是模型问题还是程序问题」。读完整条链路后判定**主因在程序侧**——
模型能力决定上限，但引擎把天花板压低且大量硬闸门只是防呆。用户要求按建议顺序优化。

改了三处文件：`src/services/agent.ts`、`src/store/agent.ts`、`src/services/agent.test.ts`。

- ① **逐子命令退出码（终结假成功反馈）**。`buildExecScript()` 抽成纯函数；顶层全 `;` 串联时
  每段包成 `{ cmd; echo "TERMAI_EXIT_x:seg:N:$?"; }`。`ExecResult.segmentStatus` +
  `extractResult` 解析 + `isExecOk()`（store 的 `isOk` 委托它）+ `renderSegmentStatus()` 进观察。
  `&&`/`||`/行尾 `&` 不分段；`balancedBraces()` 防 `{ }` 分组被裸 `}` 提前闭合。
- ② `digestOutput` 60 字符单行 → **240 字符 / 前 3 条 / `" | "` 拼接 / 附「（共 N 行）」**。
- ③ `OBSERVATION_MAX_CHARS` 3000 → **8000**；`allocateObservationBudget()` 取代按条数均摊
  （每条保底 800，剩余全给缺口最大者）；`sanitizeOutput(rawOut, 200)`；`focusOutput` 头尾随预算缩放。
- ⑥ 包装**不再外包 ` 2>&1`**（原先会抵消末条自己的 `2>/dev/null`）。

- 验证：**364 tests 全绿**（新增 27 条）；tsc 0 错；eslint 干净。
  其中 4 条是**端到端**：真实 `spawnSync("bash")` 执行生成的脚本，再喂 `extractResult`，
  断言「`cat 不存在文件; echo done` → exitCode 0 但 segmentStatus 第 1 段=1、isExecOk=false」。
- 入包核验（渲染层查中文串 + 结构性特征）：新包含「分段退出码：」「只反映最后一段」「:seg:」；
  三个新函数在包内连续出现（`isExecOk` → `allocateObservationBudget` → `renderSegmentStatus`），旧包 0 命中；
  `Cp=800` 常量仅新包有；旧包的 `cmd 2>&1; echo` 包装新包已消失。
  坑：数字常量被压成 `8e3`、跨模块常量变短变量名 → 只能查结构性特征，不能 grep 十进制字面量。
- 重新打包为 **dl-20260914f**：Setup `74cda7af4ae82ab44b377b45658dd68f`、便携版 `2e4e90c6b9c2dab86fbd2653b0b698ed`。

后续待办（用户未要求，未动）：P1「批量允许夹写操作」「写命令按目标分键」；
P2「Agent 独立模型配置」「结构化任务状态」「可回放评测集」。
