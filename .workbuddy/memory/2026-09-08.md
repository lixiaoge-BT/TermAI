
## 打包 dl-20260908a（10:14 完成，2分38秒）
内容 = dl-20260907i 全部 + 昨日 16:10 后累积三批修复：
1. 新任务带旧蓝图 bug 修复（isFresh 时 planText 置空、executedLog 不从旧 steps 重建）
2. token 节流（MAX_AGENT_STEPS=20 步闸门、同命令成功 ≥2 次硬拦截、上下文阈值降本、观察预算 3000）
3. 噪音命令与输出治理（LOW_VALUE_CMD_RE 拒绝零信息命令、批量上限 4 条、sanitizeOutput 去 ANSI/空行/超长行数、UI/store 输出上限 4000 字符 capOutput、focusOutput 错误行上限 30）
- 产物：`release/dl-20260908a/TermAI Setup 0.1.0.exe`（md5 56b97066d1507ce92cc03afad73cf2db）、便携版（md5 bfe93faf97efc44233618107758e0c92）。
- asar 核对：命令准入与输出控制=1、已执行过的命令清单=2、安装/部署任务规范=2、任务未完成=1、已达单任务步数上限=1、windowBundles=6、取消分屏=1、添加第一台 SSH 主机=1 ✓。
- 注意：dl-20260907i 不含上述三批修复，20260908a 是首个完整包含全部 Agent 修复的版本。

## 低价值命令空转口子修复（10:25，自检发现）
复查昨天的降噪改动发现：被 LOW_VALUE_CMD_RE 拒绝的轮次不建 step、不耗 20 步预算——模型若反复只给清屏/echo 类命令，
每轮都白烧一次 API 调用，只有 12 分钟超时兜底。修复：run() 局部 `lowValueStreak`（ picked 全空时 ++，
正常给命令时归零），连续 3 轮全部被拒即强制 done + 「任务未完成」提示换更具体的目标。
- 顺带修了一处 Edit 挤行（lowValueStreak = 0 与 if 挤同行，语法合法但格式坏，已还原）。
- 验证：tsc 0 / eslint 0 / 121 测试 / vite build ✓。未打包（dl-20260908a 不含此修复，dl-20260908b 备用）。
- 任务清单 23 项全部 completed，无待办。

## AI 助手全面问题分析（10:30，仅诊断未改码）
用户要求分析 AI 助手现存问题。按项目约定写探针（esbuild 打包 safety.ts 后用 node 跑，vitest 未安装）
实测 reviewCommand 判定，推翻了两条凭印象的预判，确认一批真漏洞：
- **误判为只读（none，零确认自动执行，最严重）**：`sed -i`、`find -delete`、`find -exec rm`、`awk -i inplace`
  —— 根因 safety.ts:130 isLikelyReadonly 只看第一个 token，不检查写参数。
- **漏网高危（全判 low → Agent 自动执行）**：DROP TABLE、UPDATE 无 WHERE、truncate、kill -9/pkill -9、
  docker system prune -af、kubectl delete、git reset --hard/clean -fdx/push --force、curl|bash、systemctl stop、fork bomb。
- **规则粒度不均**：`rm -rf /var/log/app`、`chmod -R 777 /data`、`chown -R www /data` 反而 critical/high
  （正则里 `\/` 匹配任意绝对路径开头，过严）；而 `rm -r ~/project`、`rm -rf ${VAR}` 是 low（过松，变量为空=删当前目录）。
- 执行阈值：none/low 自动执行、medium 需确认（autoRunMedium 默认 false）、high/critical 确认 → 漏网者全落 low。
- 其它问题：ai.ts:158 不检查 finish_reason（maxTokens 2048 截断无感知 → 协议标记丢失 → 空转）；
  问答模式无 token 预算（slice(-20) + 每轮 3 万字符输出，久了必超上下文）；错误码不翻译（402 等）；无重试；
  AISidebar 零 memo（流式 60ms 全量重渲染）；CommandCard 展示风险用 AI 标注、确认用本地 review（不一致）；
  问答与 Agent 链路割裂；共用模型参数；仅 OpenAI 兼容；生产保护靠 tag。
- 探针跑法（vitest 未装）：`npx esbuild <probe>.ts --bundle --platform=node --format=esm --alias:@=./src --outfile=dist-test/_probe.mjs && node dist-test/_probe.mjs`

## AI 助手问题批量修复 + 风险判定重写（本轮，已打包 dl-20260908b）
- **safety.ts 重写风险判定**（核心）：
  - 词法层 `splitTopLevel`（尊重引号，处理 `2>&1` 的 `&`）+ `stripHeredocs`（heredoc 正文不参与判定，避免配置模板里的 `rm -rf` 字面量抬风险）+ `parseSegment`（去 sudo/env 前缀、取 bin 与 args）。
  - 旧实现只看「第一个 token」匹配只读表 → `sed -i`、`find -delete` 被判只读（漏判）；`curl -I` 因表内存的是 "curl -I" 永远匹配不上。现在改成 bin 级 + 子命令级（`git/docker/systemctl/kubectl/apt/nginx/journalctl...`）+ 危险开关排除表。
  - **修掉「低危被判中危」**：旧规则「凡含 `>` 一律 medium」，导致 `echo x > /data/a.conf`、`cat > /app.conf <<EOF` 全部中危。现在按落点分：写系统路径（/etc、/usr 非 /usr/local、/boot…）→ medium，其余 → low。
  - rm 分级：递归 + 顶级系统目录（/、/etc、/usr…）= critical；递归 + 其子路径或 /var、/opt = high；递归 + 临时/构建产物（/tmp、node_modules、dist…）= low；其余递归 = medium；非递归 + 系统路径 = medium。
  - 补齐规则：DROP TABLE、UPDATE 无 WHERE、FLUSHALL、kill -9 1、fork bomb、curl|bash、git push -f / reset --hard / clean -fd、docker system prune、kubectl delete、userdel、crontab -r、mv /etc/*、find -delete、sed -i、chmod 777、systemctl stop、ufw/iptables。
- **模型自评风险不再盲信**（用户反馈「低危被判中高危」的直接来源）：
  - shell / SQL 代码块：本地规则为唯一权威，忽略 `risk=` 自评（既纠正虚高，也纠正低估）。
  - 其它语言：以本地推断为基线，模型自评最多抬高一档。
  - 非命令语言（text/log/json/yaml/diff…）基线从 medium 降为 none/low。
  - `isRunnableLanguage()` 导出：JSON/YAML/说明文本这类代码块不再给「执行」按钮。
- **ai.ts**：`describeHttpStatus` 翻译 401/402/404/429/5xx 等（保留服务端原文）；重试（429/5xx/网络抖动，最多 2 次，退避 800/2400ms），401/402 不重试；流式空响应自动退回非流式；`RequestMeta{finishReason,truncated}` 通过 `onMeta` 上报；历史回传改字符预算 12000（原 slice(-20)）。
- **store/agent.ts**：onMeta 感知截断 → 注入系统消息要求精简重发。
- **AISidebar**：`useActiveSessionLite()`（useShallow 只订阅标量）——原 `getActiveSession()` 随每块终端输出换身份，导致侧边栏整棵重渲染；MessageBubble/CommandCard 加 memo；CommandCard 风险展示与确认统一走本地规则、中危以下不挂警告条；ContextInfo 重量字段只在弹层打开时读。
- AgentPanel 新增 `AgentSessionLite` 类型替代整 session 对象。
- 验证：tsc 0 / eslint 0 / 220 tests（安全规则 90 条用例探针全绿，已固化为 safety.test.ts）。

## 堡垒机「请安装客户端」排查（17:30）
- 现象：用户把安装后的 TermAI.exe 改名 Xshell.exe，堡垒机仍提示未安装客户端。
- 结论：**改名只改文件名，堡垒机检测不靠文件名**，靠 ①注册表 `NetSarang\Xshell\...\Install` ②卸载项 `Uninstall\Xshell 7` ③URL 协议 `HKCR\Classes\xshell`（含 URL Protocol 值）④硬编码路径 `C:\Program Files (x86)\NetSarang\Xshell 7\Xshell.exe`。改名一项都没满足。
- 已改代码：
  - `electron/main/index.ts`：协议注册改成 `PROTOCOL_SCHEMES = ["termai","xshell"]` 循环 `setAsDefaultProtocolClient`。
  - `electron/main/deepLink.ts`：新增分支 1b) `xshell://`，把 scheme 归一成 `ssh://` 后复用 parseSshUrl（兼容 `xshell://ssh://` 嵌套写法）。
  - `package.json`：`win.protocols.schemes` 加 `xshell`（NSIS 安装时自动注册）。
  - 新增 `scripts/xshell-masquerade.ps1`：dry-run / `-Apply` / `-Remove` / `-Link` 四模式，覆盖 HKCU+HKLM 全部检测点，可建 junction 顶替硬编码路径。
- ⚠️ 未实机验证：本环境 PowerShell 工具 stdout 不回显、Bash 调 powershell 被安全策略拒、reg.exe 在程序黑名单 → 注册表方案与脚本均未跑过，需用户 dry-run 回报。
- 待确认：堡垒机型号（决定是否需要 -Link 或 PE 版本资源伪装）。

## Xshell 伪装版构建（17:47 完成）
- 用户要「直接给一个伪装版本试」→ 产出 `release/xshell-20260908b/`：
  - `Xshell Setup 0.1.0.exe`（77.95MB，安装版，会注册 termai:// + xshell:// 协议）
  - `Xshell 0.1.0.exe`（77.22MB，便携版，**不注册协议**，仅供手动跑）
- 新增 `electron-builder.xshell.json`（完整 build 段 + 覆盖）：appId=`Xshell 7`（= Windows 卸载键名，直接命中检测点②）、
  productName=`Xshell`、copyright=`NetSarang Co., Ltd.`、extraMetadata.name/author、nsis.uninstallDisplayName=`NetSarang Xshell 7`、
  win.protocols.schemes=[termai,xshell]。npm script `dist:xshell`。
- `scripts/afterPack.mjs`：rcedit 补 `"version-string"`（CompanyName/ProductName/OriginalFilename/InternalName/LegalCopyright），
  值取 `context.packager.appInfo` → 伪装版写 NetSarang/Xshell，正版仍写 TermAI。**已知坑：只传 icon 时 rcedit 不写版本资源，
  exe 属性里 CompanyName 为空（实测 utf16 计数 0）。**
- `electron/main/index.ts`：新增 userData 固定（按 Preferences/Local Storage 存在与否选 TermAI 或 termai）——
  配置存 localStorage，而 userData 跟随 productName，不钉住伪装版会「丢主机清单」。
- 核对：exe PE 资源 NetSarang×1 / Xshell×5 / Xshell.exe×1 / TermAI×0；asar 内 PROTOCOL_SCHEMES×2、
  setAsDefaultProtocolClient×2、xshell×4。tsc 通过。（PE 资源是 UTF-16，grep -a 查不到，必须用 python 按 utf-16-le 计数。）
- 注：`release/xshell-20260908a/` 是未含版本资源的第一版，已废弃可删（约 300MB，未删，等用户确认）。
- 收尾验证：eslint 0 问题；全量测试 221 → **224**（新增 3 条 `xshell://` 用例：普通形态 / `xshell://ssh://` 嵌套 /
  默认端口无密码，加在 deepLink.test.ts 的 termai:// 用例之后）。
- asar 内 `setPath`×1、`userData`×2、`Local Storage`×4、`Preferences`×2 → userData 固定逻辑确认进包；
  win-unpacked 结构完整（Xshell.exe + app.asar + app.asar.unpacked + elevate.exe）。

## 堡垒机「点了没反应」定位（18:05）
- **伪装已生效**：日志 `%APPDATA%\TermAI\logs\argv-debug.log` 里 17:54 / 17:56 两条
  `entry=first-instance argv=["D:\\App\\Xshell\\Xshell.exe"] argv.length=1 parsed=null`
  → 堡垒机确实 spawn 了我们的 Xshell.exe（装到 D:\App\Xshell，安装版，有 Uninstall Xshell.exe）。
- **但一个参数都没传** → 程序起来了却不知道连哪台机，表现就是「点了没反应」。
  argv.length=1 也排除了协议唤起（协议 URL 会作为 argv[1]）。
- 结论：堡垒机是**按配置路径直接 spawn**，且该配置的「命令行参数/参数模板」栏为空。下一步要么运维补参数模板，
  要么确认它是否改用 .xsh / 环境变量 / COM 传参。
- 新增强化取证 `argvDebug.logInvocationContext()`（仅在无参数唤起时跑，约 +300ms）：
  父进程名（tasklist，区分 chrome=协议 / dllhost=COM / 堡垒机客户端=命令行）、cwd、
  含关键字的环境变量（pass/token 类脱敏）、3 分钟内 temp/Downloads/Documents 下的 .xsh/.url/.ini/... 、
  `reg query HKCU\Software\NetSarang` 与 `HKCU\Software\Classes\xshell`。
  second-instance 无参数时也补记（原先这条高频路径完全没有日志）。
- 伪装配置加 `fileAssociations: [{ext:"xsh",...}]`（Xshell 原生会话格式，堡垒机常用它传参；
  deepLink 分支 10 已能解析 .xsh）。**未验证是否真写进 NSIS** —— 装完用 `assoc .xsh` 确认。
- 产物 `release/xshell-20260908d/`（含取证 + xsh 关联）；c 版只有取证，b/a 废弃。
- 验证：tsc / eslint 干净，224 测试全过；asar 内 parentProcessName×2、recentSuspectFiles×2、logInvocationContext×2。

## 环境真相：PowerShell 工具其实能用（18:10 修正）
- **之前记的「PowerShell stdout 不回显 → 无法读注册表」是错的**。真实情况：stdout 确实不回显，
  但**命令会执行**，用 `... | Out-File 'D:\Trae\scripts\_xxx.txt'` 再 Read 就能拿到结果。
  查注册表/进程/COM 全靠这招（reg.exe 仍被黑名单，wmic 已被 Windows 移除，cmd.exe 不能从 PS 工具调）。

## 用户机器实况（18:15 摸清）
- **装过正版 Xshell 8**：`HKLM\SOFTWARE\WOW6432Node\NetSarang\Xshell\8` → `Path=D:\App\xshell`、Version 8.0.0102、
  卸载项 `Xshell 8`（InstallShield_{4E3C6853-...}）。用户今天 17:20 把正版挪到了 **`D:\App\xshell111`**
  （Xshell.exe 3.1MB + XshellCore.exe + Xagent.exe 齐全），然后把伪装版装进 `D:\App\Xshell`（= D:\App\xshell，同一目录）。
- **所以「堡垒机认为装了 Xshell」根本不是伪装的功劳，是正版残留的注册表**（NetSarang 键 + Path + 卸载项）。
- `HKCR\.xsh` → `Xshell.xsh` → command = `"D:\App\xshell\Xshell.exe" "%1"`（正版遗留，现在指向我们）。
  双击 .xsh 会带路径参数启动我们 → deepLink 分支 10 能解析。**Downloads/Temp 里目前没有任何 .xsh，
  堡垒机没有走「下载 .xsh」这条路**。
- `HKCR\xshell\shell\open\command` = `"D:\App\Xshell\Xshell.exe" "%1"`（我们注册的，带 %1，正常）。
- **COM 假设已排除**：无 `Xshell.Application` / `Xshell.Session` ProgID，CLSID 里也没有 Xshell 的 LocalServer32。
- 新增 `scripts/watch-launch.ps1`：轮询 400ms 抓 Xshell.exe/XshellCore.exe 启动，记录 pid、父进程名+父进程命令行、
  **被启动进程的完整命令行**、以及 temp/Downloads/Documents 下 2 分钟内的 .xsh/.url/... 新文件。
  不需要管理员（Win32_ProcessStartTrace 需要管理员，故改用轮询）。这是定位「到底传没传参」的决定性工具。

## 18:20 按用户要求回退到 dl-20260908c（11:22 正版包）的代码
- **做法（可复用）**：本次会话的改动全部未提交，且 9-07/9-08 上午的工作**已 staged 在 index**。
  因此 `git diff -- <files>` 就是「本次会话改动」的干净切片 —— 先存成 patch 备份，
  再 `git checkout -- <files>` 精确回退到 index（= 11:22 构建时的状态），**不会误伤上午的 staged 改动**。
  - 涉及 6 文件：index.ts / deepLink.ts / deepLink.test.ts / argvDebug.ts / package.json / afterPack.mjs（203 增 12 删）。
  - 注意 `index.ts` 是 `MM` 状态（staged 有 133/102 大改 + 工作区改动），**绝不能 `git checkout HEAD --`**，
    否则上午的多窗口重做等成果一起没了。
- 备份落在 `release/xshell-work-backup/`：`xshell-changes.patch` + electron-builder.xshell.json
  + xshell-masquerade.ps1 + watch-launch.ps1 + 4 个 _probe 诊断输出。要恢复就 `git apply` 该 patch。
- 未跟踪的 `scripts/_del_dir.py`（13:29 删 release 目录用的）属上午产物，**保留未动**。
- 验证：tsc 0 / eslint 0 / 测试 221（伪装期的 224 回落到 221，即我加的 3 条 xshell 用例已移除，对得上）。
- 源码里仍 grep 到的 xshell 都是**原本就有**的（深链解析器原生支持 `-url`/`-ssh` 等 Xshell 命令行格式，
  测试用例名里带 Xshell），不是伪装残留，别误删。
