# 2026-08-25 TermAI 开发日志

## 已完成

### 菜单栏整合
- 将顶部菜单栏的「文件、编辑、视图、帮助」移除，功能统一收敛到「TermAI」下拉菜单。
- TermAI 菜单仅保留：新建窗口、新建标签页、主题功能（含深色/浅色/跟随系统子菜单）。
- 修改文件：`src/pages/Home.tsx`。

### 文件传输连接修复
- 定位到 `FileManager.tsx` 中 `sftpParams` 每次渲染都重新创建对象，导致 `useEffect` 依赖变化，引发无限重连、UI 卡死。
- 使用 `useMemo` 固化 `sftpParams`；并对 `host.port` 做字符串/数字兼容处理。
- 修正本地默认路径初始化逻辑（去除错误的运算符优先级）。
- 将 SFTP 连接逻辑提取为 `connectRemote`，刷新按钮在未连接时可触发重连。
- 在 `electron/main/ssh.ts` 的 `sftpConnect` 增加日志与 settled 保护，避免重复 resolve/reject。
- 修改文件：`src/components/FileManager.tsx`、`electron/main/ssh.ts`。

### 构建修复
- 修复生产构建时 `@/pages/Home` 路径别名无法解析的问题：在 `vite.config.ts` 中显式配置 `resolve.alias`。
- 恢复 `package.json` 的 `build.win.target` 为 `[nsis, portable]` 双目标。

## 待观察
- 文件传输在修复无限重连后，需在实际 SSH 环境中验证远程 SFTP 子系统连接是否正常。
